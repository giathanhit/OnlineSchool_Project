@using PagedList.Core.Mvc;
@model PagedList.Core.IPagedList<OnlineSchool_Project.Models.HocVien>
@{
    int CurrentPage = ViewBag.CurrentPage;
    ViewData["Title"] = "Thông tin học viên";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}
<partial name="_Notifycation" />
<h4 class="fw-bold py-3 mb-4"><span class="text-muted fw-light">Học viên/</span> Danh sách</h4>

<!-- Basic Bootstrap Table -->
<div class="card">
    <h5 class="card-header">DANH SÁCH HỌC VIÊN</h5>
    <div class="table-responsive text-nowrap" style="min-height: 30rem;">
        <a class="dropdown-item" href="/AdminHocVien/Create"><i class="bx bx-edit-alt me-1"></i> Thêm mới</a>
        <table class="table">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Họ và tên</th>
                    <th>Giới tính</th>
                    <th>Email</th>
                    <th>Ngày sinh</th>
                    <th>SĐT</th>
                    <th>Bằng cấp</th>
                    <th>Địa chỉ</th>
                    <th>Hình ảnh</th>
                </tr>
            </thead>
            <tbody class="table-border-bottom-0">
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Id</td>
                        <td>@item.HoTen</td>
                        <td>@item.GioiTinh</td>
                        <td>@item.Email</td>
                        <td>@item.NgaySinh.ToString("dd/MM/yyyy")</td>
                        <td>@item.Sdt</td>
                        <td>@item.BangCap</td>
                        <td>@item.DiaChi</td>
                        <td>
                            <ul class="list-unstyled users-list m-0 avatar-group d-flex align-items-center">
                                <li>
                                    <img src="~/images/hocvien/@item.UrlImage" style="max-width: 100px" alt="">
                                </li>
                            </ul>
                        </td>
                        <td>
                            <div class="dropdown">
                                <button type="button" class="btn p-0 dropdown-toggle hide-arrow" data-bs-toggle="dropdown">
                                    <i class="bx bx-dots-vertical-rounded"></i>
                                </button>
                                <div class="dropdown-menu">
                                    <a class="dropdown-item" href="/AdminHocVien/Edit/@item.Id"><i class="bx bx-edit-alt me-1"></i> Chỉnh sửa</a>
                                    <a class="dropdown-item" href="/AdminHocVien/Delete/@item.Id"><i class="bx bx-trash me-1"></i> Xóa</a>
                                </div>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<div class="mt-10">
    <div aria-label="Page navigation example">
        <ul class="pagination">
            <pager class="pager-container" list="@Model" asp-controller="AdminHocVien" asp-action="Index" />
        </ul>
    </div>
</div>
